# Text Extraction

This module is dedicated to algorithms that perform text extraction from document images using OCR (Optical Character Recognition).

All the methods follow the standard pattern of being image filters, i.e., all methods must have an image as input and offer at least one image as output.

Additional data can be provided, depending on the method, which can be seen in the dedicated documentation.

::: tasks.text_extraction